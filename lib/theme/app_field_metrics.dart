import 'app_scale.dart';

/// Padding horizontal standar field (AppTextField, picker, dropdown).
double appFieldHorizontalPadding() => size(16);

/// Padding vertical standar field (AppTextField, picker, dropdown).
double appFieldVerticalPadding() => sizeHeight(12);

/// Tinggi minimum standar field trigger.
double appFieldMinHeight() => sizeHeight(44);
